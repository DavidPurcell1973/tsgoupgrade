
import * as React from 'react';

class Process {
    processID: number;
    processDisplay: string;

    coonstructor(pProcessID: number, pProcessDisplay: string)
    {
        this.processID = pProcessID;
        this.processDisplay = pProcessDisplay;
    }
  };

///const processes: Array<Process> = [];

//export processes;
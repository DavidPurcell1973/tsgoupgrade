import {StyleSheet} from 'react-native';

export const colors = {
  primary: '#3498db',
  // secondary: '#ddd',
  header: '#3498db',
};

const styles = StyleSheet.create({
  overlayContainer: {
    width: '80%',
    height: '30%',
  },
});

export default styles;
